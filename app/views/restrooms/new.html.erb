<!-- Validation failure messages -->
<% if @restroom.errors.any? %>
  <% @restroom.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Restroom</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_restroom" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for location -->
      <div class="form-group">
        <label for="location" class="control-label">
          Location
        </label>

        <input type="text" id="location" name="location" placeholder="Location" class="form-control" value="<%= @restroom.location %>">
      </div>

      <!-- Label and input for standalone -->
      <div class="checkbox">
        <label for="standalone">
          <input type="checkbox" id="standalone" name="standalone" value="1" <%= "checked" if @restroom.standalone %>>
          Standalone
        </label>
      </div>

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @restroom.name %>">
      </div>

      <button class="btn btn-success">
        Create Restroom
      </button>
      or
      <a href="/restrooms">Cancel</a>
    </form>
  </div>
</div>
